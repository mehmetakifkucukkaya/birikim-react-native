rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Test amaçlı herkese okuma/yazma izni ver (30 gün süreyle)
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2025, 10, 8);
    }
  }
}
